# PROTOTYPE — environments overview

Throwaway. Answers: **how should the main ServiceNow Environments screen look?**

Open `index.html` in a browser (double-click, or `open prototypes/environments-overview/index.html`).

- `?variant=A` — Environment columns  
- `?variant=B` — Signal × Environment matrix  
- `?variant=C` — Sidebar + Environment detail (closest to waku shell)

Arrow keys / bottom bar cycle variants. Theme toggle in the banner. Colours are waku tokens; real UI target is GPUI (ADR-0001).
