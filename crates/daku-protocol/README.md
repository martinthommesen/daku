# daku-protocol

Versioned, transport-neutral wire contract shared by the daku client and daemon.
Serde envelopes only — no database, filesystem, or socket I/O.

TypeScript export (`export_types`) is stubbed in the hollow stack; native-only
v1 does not ship a web client.
